spring:

  application:
    name: codex-eureka

  cloud:
    config:
      uri: http://config-server:8081

  boot:
    admin:
      client:
        url: http://admin-server:8079

logging.level.org.springframework: INFO

